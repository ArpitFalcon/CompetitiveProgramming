/******************************************
* AUTHOR : ARPIT *
* NICK : arpitfalcon *
* INSTITUTION : BIT MESRA *
* Harwork is the key to success. *
******************************************/

vector<int> permutations;
for (int i = 0; i < n; i++) {
    permutations.push_back(i);
}

do {
    // Process the permutations
} while (next_permutation(permutations.begin(), permutations.end()));
// next_permtation function converts the array into its next permutation.
// Changes the existing array.   
